# kyoten — 作業のきまり

自分と AI の行動記録を集めて、そこから**相棒（アイボ）**を育てるための道具箱。

アイボは polidog のクローンではなく、**横で見ていたやつ**。
「きのう、あれで3回つまずいてたよ」と言える側で、
人格は設定ファイルではなく**記録から育つ**。

下敷きは [kani.show](https://kani.show/)（カニ省の長屋）。
間取りの考え方と、「性格をプロンプトに書かず外付けで育てる」という判断は
そこから借りた。

設計図: https://claude.ai/code/artifact/08ea4f4f-4ebf-4764-a404-69a1fb8ed669

## 何より先に: コードとデータを混ぜない

| | 場所 | 公開 |
|---|---|---|
| コード（このリポジトリ） | `~/ghq/github.com/polidog/kyoten` | してよい |
| データ（拠点） | `~/Documents/Obsidian/kyoten` | **絶対に private** |

拠点には会話の原文がそのまま入っている。取引先の実名、メールアドレス、
Slack スレッド全文、認証まわりの試行錯誤——全部入っている前提で扱う。

**このリポジトリに拠点の中身を貼らない。コミットしない。ログとして出力しない。**
動作確認で拠点の中身を見るのは構わないが、会話に長く引用しないこと。

## 間取り

拠点の部屋と、それを書く道具。**1つの部屋の書き手は1つ**。

| 部屋 | なかみ | 書き手 |
|---|---|---|
| `会話/` | 会話の原文（Claude Code / Codex） | `bin/sessions.ts` |
| `自分/` | polidog の発言だけ | `bin/me.ts` |
| `アイボ/` | AI が何をしたか（数・道具の札・言われたこと） | `bin/aibo.ts` |
| `日記/` | アイボが書く1日のまとめ | `bin/diary.ts` |
| `よその日記/` | 同じ日を、よその犬が外から書いたもの | `bin/guest.ts` |
| `出来事/` | アイボが書く1か月の年表（日付・名前・一言） | `bin/events.ts` |
| `投稿/` | polidog.jp・Bluesky・Misskey・X | `bin/posts.ts` |
| `作業/` | コミットと、詰まったこと | `bin/work.ts` |
| `ニュース/` | その日、外で話されていたこと（IT） | `bin/news.ts` |
| `おすすめ/` | その話題から、アイボが数本えらんで理由を言ったもの | `bin/picks.ts` |
| `読んだ/` | polidog がその日に開いた・シェアした先 | `bin/reading.ts` |
| `株/` | 保有株の確定した終値と評価額 | `bin/stock.ts` |
| `見立て/` | アイボがその値を見て言ったこと | `bin/outlook.ts` |
| `手帳/` | polidog が手で書く紙（受験・暮らしの控え） | **polidog**（手書き） |
| `アーカイブ/reading-notes/` | 以前の Obsidian 保管庫（原文のまま） | `bin/archive.ts` |
| `事典/` | プロジェクトごとの台帳 | `bin/entities.ts` |
| `プロフィール/` | いまの自分・`スキル/`・`年表/` | `bin/profile.ts` |
| `プロフィール/推移/` | 週ごとの累計とブログのネタ | `bin/trend.ts` |
| `週報/` | 週ごとの観測 | `bin/weekly.ts` |
| `素材/` | 機械ごとの中間物（部屋ではない） | 1階の道具ぜんぶ |

読むだけの道具（拠点に一切書かない）:

| 道具 | 何をするか |
|---|---|
| `bin/search.ts` | 全文検索。アーカイブを含め、拠点に写した場所だけが引ける |
| `bin/web.ts` | ブラウザで拠点を読む。まとめ・一覧・原文・検索（127.0.0.1 固定） |
| `bin/browse.ts` | 端末で拠点を歩く。同じものを同じ読み取り層から出す |
| `bin/hook.ts` | Claude Code の hooks から拠点を思い出させる |
| `bin/doctor.ts` | この機械の具合を見る。何が足りないか名指しする |
| `bin/nightly.ts` | 定時便。全部流して拠点を git commit する |

共通部品は `bin/util.ts`、引数まわりは `bin/cli.ts`、
拠点を読む層は `bin/read.ts`、機械ごとの素材置き場は `bin/machine.ts`。
どの機械で何を走らせるかは `config.ts`（唯一手で書く設定）。

素材は下から上へ流れる。いちばん下に `素材/` がある（**0階**。機械ごとの
取り分で、部屋ではない。拠点を1台でしか使わないなら、あることを忘れていてよい）。
1階（会話・自分・アイボ・投稿・作業・株・ニュース・
読んだ）は外から集め、**2階（事典・プロフィール・週報・推移・日記・出来事・
見立て・よその日記・おすすめ）は拠点の中しか見ない** —— jsonl も git も直接は
読まない。だから走らせる順番は sessions → me → aibo → posts → work → stock
→ news → reading → entities → profile → weekly → trend → diary → events →
outlook → guest → picks で固定（日記は素材が全部そろってから。出来事は日記も
読むのでその次。見立ては `株/` と、その日の日記を読むので日記のあと。
おすすめは `ニュース/` から選ぶのに `読んだ/` `事典/` `週報/` を根拠にするので
いちばん最後）。

`株/` `ニュース/` `読んだ/` だけは外から取ってくるのに 1階にいる。素材が
jsonl でも git でもなく **HTTP の向こう**（`読んだ/` は手元の Chrome の
履歴）だからで、畳む側（2階）より先に居ればいい。

**`おすすめ/` は外へ取りに行かない。** 選ぶ材料は `ニュース/` に書いてある
ものが全部で、そこから先は拠点だけを見る。同じ話題について違う一覧を持つ部屋を
作らないため（落とし穴21 と同じ形）。`株/` と `見立て/` の関係と同じで、
**数を並べる部屋と、それを見て言う部屋を同じ紙に混ぜない。**

`よその日記/` は**アイボの日記を読まない**ので日記より前でもいいが、画面では
2枚が並ぶので同じ夜にそろえる。

## 複数の PC で使う（2026-09-05）

拠点は **Obsidian Sync** で共有する。Claude/Codex の記録は**どの PC でも**
取る —— 片方でしか取らないなら、そちらでしか作業できなくなる。

### 壊れるのは4部屋だけ

`会話/` は元 jsonl と 1:1（名前は UUID）なので、2台が同じ拠点へ書いても
**足し算になる**。外から取る部屋（`投稿/` `株/` `ニュース/`）はどの機械でも
同じものが取れる。2階は拠点しか見ない。

ところが `自分/` `アイボ/` `作業/` `読んだ/` は**日ごとに1枚へ畳む**ので、
A が「A ぶんだけの 09-05」を書き、同期のあと B が「B ぶんだけの 09-05」で
上書きする —— **毎晩ピンポンする**。

jsonl そのものを拠点に載せて解決はできない。実測 `~/.claude/projects` が
261MB・`~/.codex/sessions` が 20MB で、拠点（61MB）の4倍を超える。

### だから、この4本を2段に割った

```
1階（機械ごと）  jsonl / git / Chrome  →  素材/<hostname>/<部屋>/…json
2階（拠点だけ）  素材/*/<部屋>/…        →  <部屋>/…md
```

運ぶのは素材だけ（実測 11MB）。畳むのは拠点しか見ないので、**どの機械で
走らせても同じ結果**になる —— 母艦が切り替えられる。原則5 も守れる:
`自分/` を書くのは畳む側ひとつ、`素材/<hostname>/` を書くのはその機械ひとつ。

`素材/` は**部屋ではない**。中間物なので `.gitignore` に入れてある ——
その機械で集め直せば同じものが出るし、`見た.json` は日付を持つので毎晩
変わり、追いかけると「変化が無ければコミットしない」が毎晩やぶれる。
Obsidian Sync は `.gitignore` を見ないので、同期はされる（そちらが本業）。

**名簿は手で書かない。** どの機械が居るかは `素材/` を見れば分かる（原則4）。
引退させるときは `素材/<hostname>/` を手で消す —— 追記のみの部屋を書き直す
ときと同じ作法。

### 追記のみの部屋にだけ、門を建てた

冪等な部屋（`自分/` `アイボ/` `作業/` `読んだ/`）は、片肺で畳んでも構わない
—— あとで相手の素材が届けば書き直せばいい。それが冪等ということ。

**追記のみの部屋（`日記/` `よその日記/` `出来事/` `見立て/`）は違う。**
一度書いたら直さないので、片肺の素材で書くと**その日は永久に片肺のまま**
残る。だから `株/` と同じ構えを取る —— **全機械が見終わった日しか書かない**
（`machine.ts` の `appendLimit`）。落とし穴69 と同じで、**遅れるだけで、
抜けはしない**。止めた夜は理由を名指しする:

```
diary: 2日 (new 0 / ある 2) ／ 素材が揃っているのは 2026-08-30 まで（laptop が遅れている）
```

**`おすすめ/` にだけ門を建てていない。** あそこが書くのは「その日の記録」
ではなく「きょうの話題のうち、どれが近いか」で、素材（`ニュース/`）はどの
機械でも同じ。しかも埋め戻さない部屋なので、門を建てると**相手が1日遅れて
いるあいだずっと黙る**。守れるものが小さく、失うものが大きい。

### 設定は `config.ts`（YAML ではない）

機械ごとに、どこまで走らせるかを hostname で引く。YAML にしなかったのは
原則6 —— Node に YAML のパーサが無く、自前で書くと**読めない行が黙って
落ちる**（`保有.md` を JSON から表にしたときの落とし穴70 と同じ形）。
TypeScript ならパーサが要らず、コメントが書けて、壊れていれば import した
時点で落ちる。

表に無い機械は既定（集めるだけ）で走り、走るたびに名指しする。
**畳む道具を1つも持たない機械は「集める側」**で、`--collect-only` が付く
（導出にしてあるのは、同じことを2か所に書かないため）。

拠点の git は **1台だけ**が打つ（`commit: true`）。Obsidian Sync は
ドットで始まるものを運ばないので `.git` は機械ごとに別物になり、2台で
打つと同じ内容について2本の履歴ができる。

### 建てるときは `bin/doctor.ts` と `/kyoten`

セットアップの判定は、ほとんど**機械にできる**。hostname が `config.ts` に
載っているか、timer が enabled か、`.gitignore` に `素材/` があるか ——
どれも読めば分かるので、`doctor.ts` に読ませる（**読むだけ**。拠点にも設定にも
書かない）。印は3つで、**「まだ」を「壊れている」として扱わない**（落とし穴64）:

```
✓ できている　　・ まだ（→ に直しかたが出る）　　✗ 食い違い
```

人にしかできないのは2つだけ —— **Obsidian Sync の選択同期**（GUI）と、
**hostname を何にするか**。前者は off でもエラーが出ないので、
`doctor.ts` と、畳む道具の毎晩の1行が名指しする:

```
me: 9日 (…) 素材 1台（poliomarchy） ／ poli-omarchy-intel が届いていない
```

**門はここを待たない。** `config.ts` に書いてあってもまだ一度も流していない
機械は、素材が1つも無いので待ちようがない —— 待つと、設定に1行足した瞬間に
日記が止まる（「宣言した」と「動いている」を混ぜない）。そのぶん、
届いていないことは**毎回言う**。

直す口が `/kyoten`（`skills/kyoten/SKILL.md`）。doctor を叩いて、`→` の行を
見せて、直せるものを直す。**拠点には書かない** —— あれは設定を見る口であって、
部屋を書く口ではない。

## 原則

1. **決定論的** — 同じ入力なら必ず同じ出力。生成日時など揺れる値を書かない。
2. **冪等** — 内容が変わらなければファイルに触れない（mtime も動かさない）。
3. **原文ママ** — 発話は加工しない。長大なツール出力の末尾だけ省略し、明記する。
4. **手で書かせない** — 拠点に人間が手入力する部屋を作らない。過去に3回、
   手書き前提の仕組みを作って全部止まっている（`00_思考`、Discord秘書、agent-tracer）。
   素材はすべてログから機械が起こす。

   **例外は2つ。** どちらも「毎日せっせと書き足す部屋」ではなく、
   **必要なときに1行足すだけの台帳**という形をしている —— 死んだ3例と
   違うのはそこ。

   1つめが **`株/保有.md`**（2026-09-05）。何を何株持っているかは
   どのログにも無いので、機械では起こせない。**買ったときに1行足すだけ**で、
   値も評価額も見立ても、そこから全部機械が起こす。

   **Markdown の表**にしてある。最初は JSON で置いたが、**Obsidian は
   `.json` をファイル一覧に出さない**（`showUnsupportedFiles` が既定で off）。
   唯一手で書くファイルが Obsidian で開けないのでは、拠点をベースにする
   意味が無い。そのぶん JSON の厳しさは無くなるので、**読めない行は
   飛ばさずに止める**（落とし穴70）。

   2つめが **`手帳/`**（2026-09-05）。受験や暮らしの控えを置く。`保有.md` が
   「ログのどこにも無い数」だったのに対して、こちらは**そもそもログを通らない
   もの** —— 面接の準備、家族のこと。会話にも git にも Chrome にも出てこない
   ので、機械には起こしようがない。

   `アーカイブ/` と分けたのは**書き手が違う**から。あちらは `archive.ts` が
   元の Obsidian 保管庫から写すだけの部屋で、新しく書く場所ではない
   （原則5: 1つの部屋の書き手は1つ）。`手帳/` を書くのは polidog ひとりで、
   道具は誰も書かない。

   **アイボには渡す**（`diary.ts` の素材）。ただしここは日付ごとに畳んだ
   部屋ではないので、**その日のぶんだけを切り出せない** —— 全部を**背景**
   として渡し、「その日の記録ではない／繋がらなければ触れない」を素材の側で
   名指しする（`guest.ts` のこじつけ禁止と同じ形）。更新日を frontmatter に
   手で書かせる手は取らなかった: 書き忘れた紙が黙って落ちる（落とし穴70 と
   同じ形）。

   読む口は**ブラウザの「手帳」の札と検索**。`browse.ts` の MENU には
   入れていない —— `アーカイブ/` と同じ、手で書く木なので、歩くのは
   Obsidian の側でいい。

   **死んだ3例に戻る合図はこれ。** 部屋が「毎日書かないと日記が薄くなる」
   仕組みになったら、そこで止まる。渡すのを背景に留めているのはそのため。
5. **書き込み口を絞る** — 1つの部屋の書き手は1つ。読みは全開。
6. **依存を増やさない** — TypeScript を **Node の標準ライブラリだけ**で書く。
   `npm install` は要らないし `node_modules` も無い。ビルドもしない
   （Node 24+ が `.ts` の型注釈を剥がして直接実行する）。そのぶん
   **実行時の意味を持つ構文は使えない** —— `enum`・`namespace`・
   `constructor(readonly x)`・decorator は落ちる。型検査は走らせていない
   （`tsc` を入れると依存が増える）ので、正しさは出力の突き合わせで見る。

   ただしこれは **npm の話**であって、LLM を禁じる原則ではない。
   手元の `claude` / `codex` を非対話で呼ぶのは依存を増やさない
   （`node_modules` は生えず、ビルドも増えない）。

7. **アイボの人格を設定ファイルに書かない** — 何を言うかは拠点が決める。
   コマンド側には「拠点を読め、そして喋れ」しか書かない。記録が増えれば
   言うことが変わる、が育っている証拠。

   立ち位置は `skills/aibo/stance.md` に**1枚だけ**置く。`diary.ts` と
   `/aibo` は両方そこを読む —— 相棒が2人に割れないように。書いてあるのは
   「誰で、何を断言してよくて、何を言わないか」と、**喋りかた**
   （2026-09-04 に足した）。

   口調は「どう言うか」であって「何を言うか」ではないので、ここに置いても
   記録が中身を決める線は動かない。そして**中身は甘くしない** —— 数も、
   「見た」と「読んだ」の区別も、やわらかく言うだけで引かない
   （「3回つまずいてたよ」は、やさしい言い方でも 3 回のまま）。

### `日記/` `よその日記/` `出来事/` `見立て/` `ニュース/` だけ 1・2 が成り立たない

書き手が LLM なので、同じところを二度書けば違うものが出る。`ニュース/` だけは
書き手が機械だが、**外の順位と点数があとから動く**ので同じところに落ちる。
代わりに置く原則:

> **追記のみ・一度書いたら直さない。**

落とし穴19 で守りたかったこと（過去が毎晩書き換わって「あのとき何と
言われたか」が残らなくなる）は、これで同じように守れる。書き直したい
ときは**そのファイルを手で消す** —— 道具には上書きの口を作らない。

同じ理由で、日記と出来事とニュースは「走らせた日」を見てよい（日記はきのうまで、
出来事は先月まで、ニュースはきょうを除く）。冪等が壊れる心配が無いので、
落とし穴18・19 の禁止が効かない。**よその日記は時計を見ない** —— 書けるのは
`ニュース/` のある日で、そこが「きょうを除く」をもう守っている。二度数える
必要が無い。**見立ても同じ** —— 書けるのは `株/` に値のある日だけで、`株/` が
「確定した終値しか持たない」をもう守っている。

株では追記のみが特に効く。**あとから「そのとき何と言ったか」が読めないと、
見立てはただの後知恵になる。** 毎晩書き換わる予想には値打ちが無い。

**単位が違うのは、確定する時期が違うから。** 日記は日ごと。出来事は月ごと。
年ごとにしなかったのは、**その年が終わるまで確定しない**から —— 1月に書いた
2026年の年表は、12月には嘘になっている。追記のみと両立しない。月なら
終われば確定する（kani.show の年表も月で束ねている）。

## いまどこまで来たか

- **LV1〜LV6 完了**（2026-09-03）— 1階と2階を建てた。
  `~/.claude/settings.json` に `cleanupPeriodDays: 3650` を設定済み
  （未設定だと30日で会話ログが消える）。
  polidog.jp 側には JSON の口を足した（[polidog/web#5](https://github.com/polidog/web/pull/5)・
  [#6](https://github.com/polidog/web/pull/6)、マージ済み）。Cloudflare の
  Cache Rule も設定済み。
- **TypeScript へ移行**（2026-09-03）— Python から書き直した。1本ずつ
  「同じ拠点に対して1バイトも違わない出力か」を `diff -r` で確かめてある。
- **読む口を建てた**（2026-09-04）— ブラウザ・端末・hooks の3つ。
  どれも拠点を**読むだけ**。
- **ブラウザでも潜れるようにした**（2026-09-04）— `web.ts` はまとめ1枚
  だったが、部屋の一覧・1枚の表示・原文・全文検索を足した。端末
  （`browse.ts`）と同じものを、同じ `read.ts` から出す。読み取り層に足したのは
  `docAt()`（住所から1枚）と `tree()`（畳まない部屋を歩く）の2つだけ。
  まとめの日記には**つぎに書かれる時刻**を添えてある（2026-09-05）——
  日記を書かせるのは systemd のタイマーなので、これだけは拠点の外
  （`systemctl --user show kyoten.timer`）から来る。聞けなければ何も言わない。
  ついでに**アイボにドット絵をつけた**（26×24・こま2枚＋まばたき）。日記の
  横・日記の一覧・日記1枚の3か所に座っている。絵は `web.html` の中に文字の表
  として持ち、走らせるときに `<rect>` へ組む（画像ファイルを増やさない）。
- **ドラクエをやめてアイボにした**（2026-09-04）— 部屋と道具を全部改名。
  役者表（勇者・賢者・盗賊・うらないババ・吟遊詩人）は削除。登場人物は
  **polidog とアイボの2人**だけ。理由は下の「なぜ改名したか」。
- **アイボの部屋を建てた**（2026-09-04）— `bin/aibo.ts`。相棒に自分の記録を
  持たせた。数・手下の働き・道具の札に加えて、**言われたこと**（選ばせた・
  止められた・断られた）。詳しくは下の「やる順」の 1 と 2。
- **日記を建てた**（2026-09-04）— `bin/diary.ts`。アイボが最初に**書いた**部屋。
- **看板を鑑識にした**（2026-09-04）— ブラウザの `<title>` と帯の左端だけ。
  部屋も道具も `拠点` という呼び名もそのまま。理由は下の
  「犬のメタファーは、看板までしか降ろさない」。
- **口調を足した**（2026-09-04）— `skills/aibo/stance.md` に「喋りかた」を
  1節。一人称は「ぼく」、短く常体、飾らない（顔文字・絵文字は無し）。
  声を変えるときの落とし穴は下の 50・51。試すのは
  `bin/diary.ts --try <日付>`（拠点に書かない）。
- **`/aibo` を建てた**（2026-09-04）— `skills/aibo/SKILL.md`。アイボが**喋る**口。
  立ち位置は `skills/aibo/stance.md` に1枚にまとめて、日記と共有した。
  **これでアイボの4段階は建て終わった。**
- **年表を出来事にした**（2026-09-04）— `bin/events.ts` と `出来事/`。年表は
  これまで数の集計だけで、**何があったかが1つも書いていなかった**。
  [kani.show](https://kani.show/) の年表と同じ形にした —— 月ごとに、日付と
  名前のついた出来事と一言。★ はその月でいちばん大きいもの。書き手は
  アイボなので `プロフィール/年表/`（`profile.ts`・冪等）とは部屋を分けてある。
  読むときは `web.ts` と `browse.ts` が2つを重ねて1枚の年表として出す。
- **感想を建てて、よその日記に建て替えた**（2026-09-04 → 2026-09-05）—
  最初は `bin/comment.ts` と `感想/` で、アイボの日記に**返事**をする部屋
  だった。いまは `bin/guest.ts` と `よその日記/` で、**同じ日を外から書く**
  部屋。経緯は下の「同じ日を、外から書く」に。
  **書かれた中身はここには貼らない** —— 実物は拠点で読む。
  書き手はいま `codex` 1人。gemini も試したが、無料枠の Gemini Code Assist が
  このクライアントを切っていて動かない（`IneligibleTierError`。Antigravity へ
  移れと言われる）。認証が通れば `guest.ts` の `WRITERS` に1行足すだけで入る。
- **ニュースを建てた**（2026-09-05）— `bin/news.ts` と `ニュース/`。拠点で
  唯一「polidog でも AI でもない」記録。出どころは Hacker News（Algolia の
  検索。**過去の日付で引ける**ので拠点にある日を全部埋めた）と、はてな
  ブックマークの IT（日本語。RSS が直近2日ぶんしか無いので**これから貯まる**）。
  どちらも API キー不要。**点数とブックマーク数は書かない**（落とし穴11）。
- **読んだものと、おすすめを建てた**（2026-09-05）— `bin/reading.ts` と
  `読んだ/`、`bin/picks.ts` と `おすすめ/`。`ニュース/` が「外で何が話されて
  いたか」を全部置くのに対して、`おすすめ/` は**そこから数本えらんで理由を
  言う**（`株/` と `見立て/` の関係と同じ。**外へは取りに行かない**）。
  理由を言うための根拠が `読んだ/` —— Chrome の履歴（2 プロファイル）と
  `投稿/` の外部リンクから起こす。**入れるものは allowlist で絞る**
  （落とし穴79。台帳は手で書かず、`投稿/` に出てくるホスト 500 種から起こす）。
  実測 519 ファイル・履歴 627 + 投稿 715 リンク。声は
  `skills/aibo/stance.md` のまま、おすすめのときの構えだけ
  `stance/osusume.md` に足した（**アイボは本文を読んでいない** ——
  `ニュース/` にあるのは題と URL だけなので、「見た」「読んだ」のさらに外)。
  読む口にも配線済み —— 帯に「ニュース」「読んだ」の札、一覧、そして
  **1日を開くと話題の下にアイボが座っておすすめを言う**（`株/` と `見立て/`
  とまったく同じ置きかた。話題は機械が集めたもので、おすすめはそれを見て
  言ったものなので同じ紙に混ぜない）。`おすすめ/` は札を持たない ——
  `見立て/` と同じで、ニュースの下にいるほうが読みやすい。
  端末（`browse.ts`）と全文検索も同じ。
- **X を投稿の4本目のソースにした**（2026-09-05）— `posts.ts --source x`。
  別の道具（`bin/x.ts`）にはしなかった —— `投稿/` の書き手は `posts.ts` 1つ、
  という原則5を割るため。出力は `投稿/<YYYY-MM>/x-<YYYY-MM-DD>.md` で
  Bluesky と同じ形なので、検索も web も browse も配線し直しは要らない。
  **取りに行く先だけ API ではなく手元のアーカイブ**（落とし穴59）。
  **アーカイブを入れた**（2026-09-06）—— 80,791 件・6,087 日ぶん
  （2007 〜 2026-09-05）。RT は落としてある。2回流して `upd 0` を確認済み。
  アーカイブは静止画なので、これから先の投稿は入らない（落とし穴59）。
- **株を建てた**（2026-09-05）— `bin/stock.ts` と `株/`、`bin/outlook.ts` と
  `見立て/`、それに `/kabu` と `kabu` subagent。値は依存ゼロで取れる ——
  日本株・米国株・ETF・為替・指数は Yahoo Finance の chart、日本の投資信託は
  投資信託協会の CSV（Yahoo では 404 になる。落とし穴62）。API キーは要らない。
  **`保有.md` だけ手で書く**（原則4 の唯一の例外。理由は上）。
  見立てはアイボが書くので追記のみ —— 素材に**前の見立てと、1か月前の見立て**を
  渡してあるので、外れていたら外れたと言える。それがこの部屋の値打ち。
  声は `skills/aibo/stance.md` のまま。株のときの構えだけ
  `skills/kabu/stance.md` に足した（「買え」「売れ」を言わない、
  **値動きの理由を作らない**、言い切りの予想をしない）。
  読む口にも配線済み —— ブラウザは帯に「株」の札、まとめに1枚、一覧、
  そして1日を開くと**値の下にアイボが座って見立てを言う**（日記とよそと
  同じ置きかた。値は機械が数えたもので、見立てはそれを見て言ったものなので
  同じ紙に混ぜない）。端末（`browse.ts`）と全文検索も同じ。
- **手帳を建てた**（2026-09-05）— `手帳/`。**原則4 の例外2つ目**で、
  polidog が手で書く唯一の木（`株/保有.md` に続く2枚目）。きっかけは受験の
  面接準備 —— 資料は `アーカイブ/reading-notes/` にあるが、あそこは
  `archive.ts` が写すだけの部屋なので、進み具合と成果物を置く先が無かった。
  読む口は帯の「手帳」の札・部屋を歩く口・全文検索（`--room 手帳`）。
  配線は十数行で済んでいる —— `read.ts` の `tree()` が畳まない部屋を汎用に
  歩くので、`アーカイブ/` と同じ扱いに乗るだけ。**アイボには背景として渡す**
  （`diary.ts`）。中身はここには貼らない —— private なので拠点で読む。
- **いまの規模** — 会話 360本 / 自分 9日 / アイボ 9日 / 日記 8日 /
  よその日記 8枚 / ニュース 8日 / 投稿 2,428 / 作業 858日
  （7,820コミット・66リポジトリ）/ 事典 66 / スキル 71 / 年表 23年 /
  週報 776週 / 推移 776週。索引 6,242ファイル・51,997かたまり・146MB。

### なぜ改名したか

ドラクエの語彙は**機構を持っていなかった**。うらないババがいても占いは
実装されず、吟遊詩人は名前だけ先にあった。役者が5人いるのにプロセスは
1つしかなく、名前が飾りになっていた。

アイボは違う。「行動記録から人格が育つ」は世界観であると同時に
そのままアーキテクチャなので、飾りになりようがない。1体しかいないのも
実態に合っている。

そして新しい名前は**説明が要らない名前**にした（`会話/` `自分/` `投稿/`）。
以前あった「語彙」の表は、名前が不透明だから存在していた。表ごと消えた。

### 同じ日を、外から書く

**アイボは世の中を見ない。** 見るのは拠点だけで、それは意図してそうしてある
（夜に無人で走るので、外の当たり外れを持ち込まない —— `outlook.ts` と同じ
構え）。だから日記には**その日、外で何が起きていたか**が一行も無い。

`よその日記/` がそこを引き受ける。渡すのは `ニュース/` と、その日の記録
（`アイボ/` `自分/` `作業/` `投稿/`）。同じ日について、**中から1枚と外から
1枚**が並ぶ。

**アイボの日記は渡さない。** 読ませると日記ではなく返事になる。

#### 感想（返事）から、日記（並走）へ替えた（2026-09-05）

前身は `comment.ts` と `感想/` で、アイボの日記を読んで返す部屋だった。
二段階で替えている:

1. **日記1枚だけ → 素材つき**（同日）。「知らないことが役どころ」という作りは
   効いていたが、出てくるのは「読めません」「書かれていません」ばかりで、
   **穴は指せても、穴の向こうに何があったかは言えない**。素材を渡したら
   「記録にはこうあるのに、書かなかった」に変わり、未決着を完了として畳んだ
   日や、因果の時刻が逆な箇所まで指せるようになった。
2. **返事 → 日記**（同日）。それでも返事であるかぎり、**アイボの一日の枠の
   中**でしか喋れない。アイボが書かなかった話は指せても、**アイボが見て
   いない世界**は持ち出せない。そこで `ニュース/` を建てて渡し、返すのを
   やめて日記にした。

**替えて分かったのは、独立の根拠がどこにあったか。** 「知らないこと」だと
思っていたが、渡すものを増やしても犬は割れなかった。効いていたのは
**「書いていないこと」**、そして最後は **「中にいないこと」**だった。

立ち会ってはいないので「見た」と「読んだ」を分ける（アイボが `会話/` を
断言して `投稿/` を伝聞で言うのと同じ線を、よその犬は素材ぜんぶに引く）。
**ニュースは見出しだけ**渡しているので、中身を知っているふりもさせない。

**いちばん効いた決まりは「こじつけを書かない」。** その日の話題と polidog の
作業が繋がっていない日のほうが多い。「奇しくも同じ日に」を禁じて、
**繋がっていなければ繋がっていないと書け**とした。`見立て/` の「値動きの
理由を作らない」と同じ形。実測では毎枚どこかで「直接つながっていません」と
言っていて、そのうえで**外から見ると何が大きく見えたか**を書いている。

素材を渡すと**声が薄まる**ので、`diary.ts` と同じ手当てが要る ——
立ち位置から `## 喋りかた` を切り出して、素材の**後ろにもう一度**置く
（落とし穴53）。日記1枚のころは千字ちょっとだったので要らなかった。

**声は分けてある**（アイボは常体、よその犬は ですます）—— 同じ画面に並ぶので、
混ざると誰の声か分からなくなる。

**それでもアイボは1匹のまま。よその犬も1匹。** 書き手はモデルごとに個体を
分けない —— `アイボ/` が Claude Code と Codex の記録をまとめて食べるのと同じ
考えかた。立ち位置は `stance/guest.md` に1枚だけ置く。

### 犬のメタファーは、看板までしか降ろさない

ブラウザの看板は **鑑識**（`<title>` と帯の左端。2026-09-04）。下敷きの
kani.show が「カニ省」という**役所**で立っているのに対して、こちらは
**現場の課**で受ける。これは飾りではなく、実際にやっていることの名前
になっている —— **本人には聞かず、残った痕跡から起こす**（原則4）。
「拠点には無い」と言うのも、鑑識なら当たり前の作法。

**降ろすのはここまで。** 部屋（`会話/` `自分/` …）と道具（`search.ts` …）には
犬の語彙を持ち込まない。`search.ts` を「臭気選別」、`nightly.ts` を「夜回り」に
すると機構は噛むが、**説明が要らない名前**という基準を割る —— 役者表を消した
ときと同じ失敗になる。メタファーが置けるのは**看板1枚**と、**立ち位置の紙**
（`skills/aibo/stance.md`・`stance/guest.md`）まで。

そして「証拠」「対象」「取り調べ」は使わない。調べる相手は自分自身なので、
そこを警察の語彙で埋めると、相棒が取調官になる。アイボは横にいる。

### 犬は増やしてよい。ただし機構1つにつき1匹（2026-09-05）

アイボのほかに **レトリバー**（ゴールデンレトリバー）がいる。同じ日を
外から書く、よその家の犬（`stance/guest.md`・`bin/guest.ts`）。

役者表（勇者・賢者・うらないババ）を消したのは、**名前だけで機構が無かった**
から。レトリバーは違う —— `guest.ts` が本当に別のプロセスを立てて、
**アイボの日記を渡さずに**同じ日を書かせている。「中にいない」は演出では
なく、実際にそうなっている。だから名前が飾りにならない。

役どころは2度替えたが（感想 → 素材つき感想 → よその日記）、**犬は1匹のまま**。
替えて分かったのは、独立の根拠が「知らないこと」ではなく
**「中にいないこと」**だったということ。

判定はこの1つ:

> **その名前の裏に、独立したプロセスがあるか。**

無ければ足さない。あれば、犬でよい。読者を走らせる機械が増えても
（codex に gemini が並んでも）**レトリバーは1匹のまま** —— アイボが
Claude Code と Codex の記録をまとめて食べるのと同じで、機械の名前は
`by:` に残す。犬が増えるのは、**役どころが増えたとき**だけ。

#### シェパードからレトリバーに替えた（2026-09-05）

最初はシェパードだった。番犬・警察犬の顔で、「外から指す」役には合うが、
**役どころと噛んでいなかった**。この犬がやっているのは見張りではなく、
**外で話されていたことを拾って、この家まで運ぶ**こと（`ニュース/` を
渡されて、アイボが見ない世の中を持ち込む）。それはそのままレトリバー
（retrieve = 持ってくる）なので、名前が機構の説明になる —— 看板の
「鑑識」と同じ基準。

ついでに、**本文にも犬を少し降ろした**。立ち位置の紙に「犬として」の節を
1つ足して、くわえる・鼻・耳・しっぽ・庭までは言ってよいことにした。
ただし**1枚に2つまで**（3つ目からは飾り）、**記録の外を嗅がない**（においが
しても、無ければ「記録には無い」）、そして**犬語は喋らない**のはそのまま。
犬が出ても中身は引かない —— 数も、「読んだ」と「見た」の区別も、
「繋がっていない」もそのまま運ぶ。これは原則7 の「口調は『どう言うか』で
あって『何を言うか』ではない」の線と同じところに立っている。

本文に犬が出るのは、看板の外に降ろしたのではない。**立ち位置の紙**から
出ているので、「メタファーが置けるのは看板1枚と立ち位置の紙まで」は
守れている。部屋と道具の名前は触っていない。

## 落とし穴（同じところを踏まない）

### 会話ログを読む

1. **`session_id` の先頭8文字は一意ではない。** `52de10eb` で始まる別セッションが
   実在した。サブエージェントは親と同じ `sessionId` を持つ（最大18ファイル）。
   出力ファイル名は**元 jsonl と 1:1** にする。
2. **universal newlines で冪等性が壊れる。** CR を含むログがある（実測10件）。
   読み書きとも改行を変換しない（Node は既定でそうなっている）。
3. **MCP の戻り値は `\uXXXX` エスケープされた JSON。** 開かないと Obsidian で読めず、
   全文検索でも日本語が引けない。開いてから書く。
4. **`jq ... | wc -l` は行数であって件数ではない。** 実測 30,887行 = 888件。
   数を報告する前に、何を数えているか確かめる。
5. **「ユーザー行」は本人の発話ではない。** 実測1,066行のうち本人の入力は約550。
   `isMeta` / `isCompactSummary` / `isSidechain` / `origin.kind` / `promptSource`
   で落ちるものはフラグで落とす。文面判定はフラグの無い古いログ用の最後の砦。
6. **スラッシュコマンドの `<command-args>` は本人の言葉。** `/omarchy フォントを…`
   のように引数に本文が入る（実測66件）。コマンド名だけの行は捨て、引数は拾う。
7. **`is_error` は「詰まった」ではない。** `ls … && wc …` の後半だけこけても
   is_error になる。長さとエラー語で絞る（実測 117件 → 41件）。Codex は
   `status: "failed"` と `exit_code` を明示するので合図は確実だが、
   「非ゼロでも中身は普通の出力」は同じなので絞り込みは共通のものを通す。

### 外から取ってくる

8. **polidog.jp は Hugo ではない。** `polidog/website`（Hugo）は旧サイトで、
   公開中の記事と 1 本も一致しない（実測: サイトの 8月記事 11本 vs ローカル 3本、
   重なり 0）。現行は `polidog/web` —— Relayer 製の自前 CMS、記事は SQLite。
9. **Cloudflare は `Accept` をキャッシュキーに入れない。** `Vary: Accept` も
   見ない。同じ URL で HTML と JSON を出し分けるなら、JSON を `no-store` に
   したうえで Cloudflare 側に bypass ルールが要る。無いとエッジの HTML が
   JSON 要求にも HIT する（実測: `cf-cache-status: HIT` で HTML が返った）。
   ルールは「Eligible for cache」より**下**に置く（最後に一致したものが勝つ）。
   そして **`cf-cache-status: BYPASS` は効いている証拠にならない** ——
   アプリが `no-store` を返しただけでも BYPASS と表示される。確かめるには
   記事 URL を HTML で 2 回叩いて `HIT` にしてから JSON を要求する。
10. **索引に載っているのに取れない記事があった。** 記事 URL には
    `/YYYY/MM/DD/slug`（1,294本）と `/blog/YYYY/MM/slug`（12本）が同居していて、
    `/2006/10/16` のようなものまである。web 側が URL の形で判定していたので
    12本が HTML を返していた（[polidog/web#6](https://github.com/polidog/web/pull/6) で修正）。
11. **SNS のいいね数・リアクション数を書かない。** 過去の投稿でも増減するので、
    書くと毎回全ファイルが書き換わって冪等が壊れる。
12. **Bluesky の `record.text` は URL が省略表示。** 実 URL は `facets` と
    `embed` にある。本文は原文ママのまま、URL を後ろに添える（実測 100件中 27件）。
13. **日本語スラッグの URL はエンコードが要る。** 248本ある。
14. **「取れなかった」と「0件だった」を混同しない。** 落ちている日に空ファイルを
    書くと過去が消える。取りに行けなかったソースはファイルに触れずに諦める。
    ただし記事は 1本ずつ独立なので、1本落ちても残りは書く。

59. **X の無料 API はもう無い。** 2026-02-06 に無料枠が廃止され、投稿の読み取りは
    1件 $0.005（$5/1,000件）の従量課金になった。廃止前の無料枠も読みは
    50件/24h で、20年ぶんには最初から足りていない。毎晩走る道具に課金の口を
    持たせないため、**X だけ公式アーカイブ**（設定 → データのアーカイブを
    ダウンロード）を読む。代わりにこれから先の投稿は入らない —— 静止画なので
    `archive.ts` に近い立ち位置になる。
60. **`tweets.js` は JSON ではなく JS の代入文。** `window.YTD.tweets.part0 = [ … ]`。
    `=` の後ろの `[` から先は素の JSON。数が多いと `tweets-part1.js` … に割れるので
    **番号順**に並べる（文字列順だと part10 が part2 より前に来る）。
61. **アーカイブの `retweeted` はリツイートでも false。** 落とす合図に使えない。
    見分けるのは本文の `RT @name: `。しかも RT の本文はアーカイブの時点で
    切り詰められているので、拾っても原文にならない（落とし穴7 と同じ形 ——
    フィールドの名前と、実際に入っているものが違う）。
62. **`full_text` は t.co に短縮され、`&` `<` `>` がエスケープされている。**
    実 URL は `entities.urls[].expanded_url`（落とし穴12 の Bluesky とまったく
    同じ形）。エスケープは運び方の都合なので開く（落とし穴3 と同じ理由）が、
    **`&amp;` は最後** —— 先に開くと `&amp;lt;` が `<` まで戻る。
    そして `created_at` は UTC（`Wed Oct 10 20:19:24 +0000 2018`）なので、
    JST で日付が1日ずれる。
83. **アーカイブの `created_at` が壊れているものがある。** 実測 80,791 件中
    2 件が 1998 年と 2001 年 —— X が生まれる前で、そのまま書くと
    `投稿/1998-08/` ができた。ID は snowflake（上位ビットが ms の時刻）なので
    本当の時刻（どちらも 2011-12）を持っている。**1日以上食い違うときだけ**
    ID の時刻を採る（`posts.ts` の `xTime`）。常に ID を採らないのは、残り
    76,653 件が 1 分の内で一致していて、秒の桁で全件が動くのを避けるため。

73. **HN の Algolia は `>` `<` を素で URL に入れると 400。** `numericFilters`
    に不等号が要るので、`URLSearchParams` か `--data-urlencode` で組む。
    そして **`search_by_date` ではなく `search`** —— 前者は新しい順なので、
    その日いちばん話されたものが取れない。日付で挟んで `search` を叩くと
    点数順に返る（実測 2026-09-02 は 1,223本）。
74. **ニュースの点数とブックマーク数は書かない**（落とし穴11 の再演）。
    どちらもあとから増えるので、書くと同じ日が毎晩書き換わる。**順位で
    大きさを表す**（取るときには使う。書かないだけ）。それでも順位自体が
    動くので、`ニュース/` は冪等ではなく**追記のみ**。
75. **はてなブックマークの RSS は直近2日ぶんしか無い。** 過去は埋まらないので、
    HN だけの日ができる（実測 2026-08-28 〜 09-02 は HN のみ）。取れないのと
    0件は違うので、**空の節は書かない**（落とし穴14 と同じ形）。逆に HN が
    落ちた日は**ファイルごと書かない** —— 追記のみなので、空の1枚を置くと
    その日は永久に空のままになる。

### 数える・畳む

15. **モノレポの奥で作業した日は別プロジェクトに見える。** `slugFromCwd()` は
    cwd をそのまま名前にするので `<repo>/apps/web` と `<repo>` に割れる。
    git 側は常にルートを名乗るので、放っておくと同じ日の別々の見出しになる。
16. **擬似プロジェクトの名前で全文検索しない。** `Work` や `_home` は名前が
    ただの単語なので、投稿を部分一致で総なめにする（実測: `Work` が
    "work" を含む記事87本を拾っていた）。探すのは `<user>/<repo>` の形だけ。
17. **git worktree を二重に数える。** worktree は本体と同じ履歴を持つので、
    両方走査すると同じコミットが2回入る（実測 8,320件中 524件）。本体の
    `.git` はディレクトリ、worktree はファイルなので `isDirectory()` で分ける。
18. **「直近N日」を今日から数えない。** 素材が変わっていないのに日をまたぐ
    だけで中身が動き、冪等が壊れる。拠点にある最後の日から遡る。
19. **「N日ぶり」「N日止まっている」を今日から数えない。** 過去の週報が毎晩
    書き換わり、「あのとき何と言われたか」が残らなくなる。週を古い順に見て、
    その週の終わりまでに分かっていたことだけで書く。
20. **推移の累計は「拠点にあるもの」の累計であって、起きたことの累計では
    ない。** 会話は 30日で消えたぶんが手元に無いので、写しを取り始める前の
    週は 0 のまま積み上がらない（実測: 保全を始めた週に 262本が一気に入った）。
    記事とコミットは最初から全部あるので素直に読める。
21. **素材の読み方を2つ持つと、同じ週について違う数を言う部屋ができる。**
    推移は週報の `scan` / `fold` をそのまま借りる。借りる側ができたので
    `weekly.ts` は `import.meta.main` で守った（守らないと import した瞬間に
    776週を書き直しにいく）。**2026-09-05 に同じ穴を踏み直した** —— まだ
    守っていない `reading.ts` を、確認のつもりで `node -e 'import(…)'` した
    ら引数なしの本番実行になり、605 ファイルが拠点に積まれた。
    **書く道具は、書いた直後に守る。**
22. **畳み方を直しても、前に書いたファイルは残る。** `feat-441` のような枝を
    親に畳むようにしても、畳む前に書いた台帳は誰も上書きしないので居座る
    （実測1件。数だけ 67 と 66 で食い違って気づいた）。道具は消さないので、
    畳み方を変えた回は手で片付ける。

### 検索

23. **FTS5 trigram は3文字未満を索引に入れられない。** 日本語は「拠点」「記憶」
    のような2文字語が多く、`MATCH` では1件も引けない。素の部分一致に落とす。
    `snippet()` も 1トークン=3文字で窓が狭すぎるので抜粋は自前で切る。
24. **検索の報告は stdout ではなく stderr に出る。** 検索結果だけを stdout に
    流す作りなので、`| grep` しても混ざらない。定時便が「何も言わずに終わった」と
    言い出したらこれ。
25. **`execFileSync` は成功時の stderr を返さない。** 検索は stderr に報告
    するので、定時便から呼ぶと黙って終わったことにされる。`spawnSync` を使う。

### 定時便・git

26. **systemd user service の PATH は最小。** git を呼ぶので `/usr/bin` が要る。
    `Environment=PATH=` で明示する。
27. **`git status --porcelain` は未追跡ディレクトリを1行にまとめる。**
    そのまま数えると「投稿 1」という嘘の記録になる。`--untracked-files=all` を付ける。
28. **git は ASCII でないパスを8進エスケープして返す。** 部屋の名前を日本語に
    した回、`プロフィール/` で始まるかを見ても1件も当たらず、5,358件の変更を
    「変化なし」と言った。`-c core.quotepath=false` を付ける。
29. **改名は `R  <前> -> <後>` の1行で来る。** 前だけ見ると、部屋ごと名前を
    変えた回にコミットを飛ばす。矢印があれば後ろを取る。

### 書く・描く

30. **JS の `.` は `\r` にマッチしない**（Python の `.` はする）。会話ログには
    CR があるので `^## (.+)$` だと見出しを取りこぼす。`[^\n]` で書く。
31. **`.slice(0, n)` は UTF-16 単位。** Python の `[:n]` はコードポイント。
    絵文字が混ざると1文字ずれる。`util.ts` の `take()` を使う。
    同じ理由で `padEnd()` は日本語の枠を揃えられない。
32. **端末は複数のキーを1回の `data` で渡してくる。** 速く打つと `↓↓q` が
    1つの塊で届く。塊のまま比べるとどのキーにも当たらず、**全部無視される**。
    キーに割ってから1つずつ流す。割るときは `[...chunk]` で数える（31）。
33. **描くのを最後の1回にまとめると、キーの処理が古い画面を見る。** 塊で
    届いた `Enter` が、前の画面のカーソル行を開いた。キーごとに、いま居る
    場所のページを引き直す（同じ場所ならメモが効くので安い）。
34. **`listen()` は非同期。** ほかの道具と同じ `process.exit(main())` の形で
    書くと、サーバは立ち上がった端から閉じる。対話するものは戻り値で終わらせない。
35. **拠点の中身を画面に描くときは必ずエスケープする。** 会話原文には HTML も
    `<script>` も普通に入っている。検索語を光らせるところも、印を入れてから
    エスケープすると印まで消えるので、一致箇所で切って組み立てる。

### AI 側を数える

40. **Claude Code は1つの応答をブロックごとに別の行で書く。** 同じ
    `message.id` を持つ行がいくつも並ぶので、そこで重ねて数えると道具
    呼び出しがまとめて消える（実測 8,041 回が 3,016 回に減った）。
    `--resume` の写しを落とす鍵は **行の `uuid`**（`me.ts` と同じ）。
41. **`会話/` の `replies` は「発言の数」ではない。** あちらは jsonl の行を
    数えるので思考も道具も入る。`アイボ/` が数えるのは text ブロックだけで、
    同じ日について違う数になる。だから名前も `speech` と分けてある（同じ
    ものを指す別の数を2つ持たない、が守れないときは名前で分ける）。
42. **assistant の発言を写すと索引が二重になる。** 実測 890,227 文字が
    まるごと `会話/` にある。`アイボ/` が持つのは、ほかの部屋に無いものだけ。
43. **道具の札に生のコマンドを使わない。** `アイボ/` は全呼び出しを並べるので、
    鍵の入ったコマンドが大量に写りうる。`description`（AI 自身の要約）→
    パス、の順で取る。生のコマンドが要るときは `会話/` を引く。

44. **「直された」は文面では取れない。** 日本語では AI への依頼と AI の
    訂正が同じ動詞で書かれる（「直して」も「消して」も「戻して」も普通の
    依頼）。実測 645 発言中 51 件当たって、本物は 1〜2 割。ひらがなの部分
    一致はさらに悪く、**「まって」が「溜まって」「いまって」に当たった**。
    落とし穴7（`is_error` は「詰まった」ではない）と同じ形。拾うのは機械的な
    合図だけにする —— AskUserQuestion の答え・Esc の中断・道具の拒否。
45. **AskUserQuestion の答えは、どの部屋にも入っていなかった。** `自分/` が
    見るのは user 行の text ブロックで、答えは tool_result で来る（落とし穴5
    の「ユーザー行は本人の発話ではない」の裏側 —— 本人の言葉なのに text
    ではないものがある）。実測 104 件、中央値 230 文字、自由記述つき。
46. **`claude -p` はセッションのログを残す。** そのままだと**日記を書いた
    こと自体がその日の会話ログになり**、`会話/` と `アイボ/` に混ざって
    循環する（実測: 1回呼ぶと jsonl が1本増えた）。`--no-session-persistence`
    が要る（`--print` のときだけ効く）。hook も `--settings '{"hooks":{}}'`
    で黙らせる。
47. **mise の shim は起動通知を stdout に出す。** `mise ~/.config/mise/…
    tools: claude@…` が**日記の1行目になった**。`MISE_QUIET=1` を渡す。
    **`claude` だけの話ではない** —— `gh` でも同じものが出て、JSON が
    パースできなくなった。mise 管理の道具を別プロセスで呼ぶときは常に付ける。
48. **見出しの無い文書は `readDoc()` が本文を落とす。** `lead` は最初の1行
    しか持たず、残りは `sections` にも入らない。日記は段落しかないので、
    ブラウザにも端末にも1行しか出なかった。`Doc.body` を足した。
49. **コミット件名の `#N` は PR とは限らない。** 実測 107 件あるが、中身は
    全部 issue の参照だった（`Fix #321` は GitHub が issue を閉じる記法、
    `#332 の対応` も同じ）。PR として確実なのは
    `Merge pull request #N from …`（1,747）と、末尾の `(#N)`（791）だけ。
    混ぜると「マージした PR」が嘘になる。
50. **拠点から取れるのは「マージされた PR」だけ。** マージせずに閉じた PR は
    git に痕跡が残らない。実測で全体の 5%（4リポジトリ 554本中 26本）なので、
    そのために GitHub へ聞きに行く依存は足していない。
51. **平らに並べると、忙しいリポジトリ1つが枠を食い切る。** 週報の PR を
    番号順に12本出したら、105本あった週は上位12本が全部同じリポジトリだった。
    リポジトリごとに畳んでから出す。
52. **道具の拒否も `is_error` で来る。** 「答え」より先に「拒否」を見ないと、
    拒否まで `is_error` として落ちる。そして AskUserQuestion 自体が
    バリデーションで落ちた回（`<tool_use_error>`）を答えに数えると、
    決めていないものを決めたことになる（実測2件）。

### 声を変える

53. **口調は先頭で1回言っても効かない。** 素材を 3万字渡すので、立ち位置の
    末尾に書いた「喋りかた」は薄まる。しかも `diary.ts` は**きのうの日記**を
    「声が続くように」渡しているので、古い声に引っぱられる（実測: 口調を
    足した直後に書かせたら、前と同じ声で出た）。素材の**後ろにもう一度**
    置く。同じ文を2か所に持たないよう、stance から `## 喋りかた` を切り出して
    使う。
54. **決まりだけでは声にならない。見本を1つ置く。** 「短く」「常体で」だけ
    では長い分析文のままだった。**形だけの見本**（中身は真似しない、と
    断ったうえでの3行）と「40字を超えたら割る」を足したら決まった。
    実測: 1文の平均 28〜30字・「ぼく」1〜2回・「！」0。
72. **見本は、中身だけでなく形も丸ごと写る。「真似しない」と書いても効かない。**
    読者の見本が「その日は見ていません。読んだのは、この一枚だけです。」で
    始まっていて、**8枚が8枚ともその1行から始まった**（締めの
    「いちばん残ったのは」も 7 枚）。54 の裏側 —— 見本は効きすぎる。
    しかも「立ち会っていないことを自分から1回は言う」を決まりにしていたので、
    見本と決まりが同じ場所を二重に押していた。**毎日同じ挨拶をする犬は、
    読んでいないのと同じに見える。** 直したのは3つ: 名乗りの決まりを消す
    （立っている場所は「書かれていません」「読めません」と言うたびに出るので
    宣言は要らない）、書き出しと締めを固定しないと書く、見本を**入り口の違う
    2つ**にして「言い回しは持ち出さない」と断る。
    **既に書いた8枚は追記のみなので直らない** —— 声を変えたら、そこから先
    だけが変わる。

### 株を見る

59. **Yahoo の日足は、引けた直後でも当日ぶんが `close: null` で返る。**
    最初は「いちばん新しいバーは必ず捨てる」で確定を守っていたが、それだと
    null を落としてからもう1本捨てるので、**確定した日まで消えた**（実測:
    東証が 15:30 に引けたあと、トヨタが 09-03 まで消えて 09-02 になった）。
    確定したかは `regularMarketTime` が `currentTradingPeriod.regular.end` に
    届いたかで見る。バーの日付がその場の日付と違えば、もう終わっている。
60. **円換算に場中のレートを使うと冪等が壊れる。** 過去の日を書くのに
    「いまの USDJPY」を掛けていて、2回流したら同じ 2026-09-03 が
    5,550,631 と 5,550,638 になった。日足はその日の確定終値なのに、
    **掛ける数だけが動いていた**。書くときは全部その日の確定値で揃える。
61. **`保有.md` は「いまの株数」しか持っていないので、過去を埋め戻さない。**
    去年の株価に今日の株数を掛けたら、持っていなかった株の評価額を
    でっち上げることになる。書くのは拠点にある最後の日より先だけ
    （落とし穴18 の形）。だから `株/` は実質**追記のみ** —— あとで買い増しても
    書いてある日は直さない。何も書いていなければ、確定した最新の1日だけ書く。
62. **日本の投資信託は Yahoo の口では引けない。** 協会コードも `.T` を付けた
    ものも 404（実測 `0331418A` / `0331418A.T` / `03311187`）。投資信託協会の
    CSV（`toushin-lib.fwg.ne.jp`）へ行く。**Shift_JIS** なので
    `TextDecoder("shift_jis")` で開く。**ISIN と協会コードの両方が要る**。
    こちらは確定した基準価額しか載らないので、Yahoo と違って最後の行は捨てない
    （捨てると投信だけ常に1日古くなり、全体の書ける日を無駄に引き戻す）。
    そして**基準価額は1万口あたり** —— 掛ける前に単位を揃える。
63. **書いていない `取得単価` を 0 にしない。0 も「書いていない」扱いにする。** 0 にすると取得額が 0 円になり、
    **損益が評価額まるごとになる**（実測: ACCESS を株数だけ書いて流したら
    +42,100円 と出た。ただ持っているだけなのに）。**買値が 0 円の持ち株は
    無い**ので、雛形の `"取得単価": 0` を埋め忘れたまま流しても嘘にならない
    ようにしてある。外貨建てなら
    `取得レート` も同じで、いまのレートで割り戻すと嘘になる。どちらか欠けたら
    **円建ての損益は出さない**（合計にも出さない。何が足りないかを名指しする）。
    落とし穴14 と同じ形で、「取れなかった」を 0 で埋めない。
64. **「まだ」と「壊れている」を分ける。状態は3つある。** `保有.md` が
    **無い**のも、あるが**株数が書きかけ**なのも失敗ではないので 0 で終わる
    （定時便に組んであるので、埋めるまで毎晩「失敗」が並ぶのは違う）。
    **JSON として読めない**ときだけ 1 で終わる。ただし 0 で終わるときも
    黙らない —— 何が足りないかを毎回名指しする（落とし穴65）。
    そして書きかけのまま値を書かない: 株数 0 で流すと**評価額 0 円の日**が
    積まれ、`株/` は追記のみなので手で消すしかなくなる。
65. **`--quiet` で何も言わないと、定時便が「何も言わずに終わった」と言う。**
    仕事が無い日に黙って 0 で終わったら、夜の記録がそれで埋まった
    （落とし穴25 と同じ形）。**何もしなかったときも1行出す。**
66. **画面が言い切っている文も、部屋を足したら見る。** まとめの冒頭に
    「**手で書いた部屋は一つもなく**、ぜんぶログから機械が起こしたもの」と
    直に書いてあり、`保有.md` を置いた時点で嘘になった。拠点の中身だけ
    見ていると気づかない —— 文は `web.html` の中にある。いまは持っているか
    どうかで言い分けて、どちらでも本当のことを言う。
67. **複数日ぶんを頼むと、Yahoo の日足は引けたあともしばらく埋まらない。**
    東証が 15:30 に引けて **9時間半たっても `close: null`** だった
    （`range=5d` も `1mo` も `3mo` も同じ。`range=1d` だけ 421 で埋まっていた
    ので、遅れているのは複数日ぶんのキャッシュのほう）。放っておくと定時便
    （03:00）がその日を書けず、**値も見立ても丸1日遅れて積まれる**。
    引けていれば `regularMarketPrice` がその場の確定した終値なので、
    **バーの無い日だけ**それで埋める（`range=1d` の値と一致することを
    確かめた）。バーがあるならそちらを優先するので、翌日に埋まっても
    値が二重にならない。落とし穴59 とは別の話 —— あちらは「まだ動いている
    バーを捨てる」で、こちらは「もう動かないのに空いている日を埋める」。
68. **`損益` は、書いていないことと 0 を分ける。** 取得単価の無い銘柄が
    あると frontmatter に `損益` が出ない。読む側で `num()` に通すと 0 に
    なり、**「損も得もしていない」に化ける**。一覧では `—` と出す
    （`read.ts` の `StockHead.gain` は `number | null`）。

69. **投信の基準価額は、株より遅れて届く。定時便の時刻はその境目にある。**
    実測（2026-09-04 ぶん）: 翌 **01:03 にはまだ 09-03 まで**しか無く、
    **08:14 には 09-04 が入っていた**。定時便は 03:00 なので、その日ぶんが
    間に合う夜と間に合わない夜がある。評価額は**全部の保有が揃った日**しか
    書かないので（揃わない日に部分的な合計を書くと嘘になる）、
    **投信を1本持っているだけで記録全体が1日ずれることがある**。
    ただし `stock.ts` は拠点にある最後の日より先を**全部**書くので、
    間に合わなかった日は次の夜に入る —— 抜けはしない、遅れるだけ。
    急いで見たいなら `stock.ts --now`（場中の値で、拠点には書かない）。

70. **手で書く台帳を Markdown にしたら、読めない行を飛ばさない。** JSON は
    壊れていれば `JSON.parse` が投げてくれたが、表は壊れた行が黙って落ちる。
    落ちると保有が1本減ったまま評価額を書き、`株/` は追記のみなので後から
    直せない。数として読めない升目があったら**その行を名指しして 1 で終わる**
    （落とし穴14 と同じ形で、「読めなかった」を「持っていない」にしない）。
    ついでに `1,234,567` や `100株` は通す —— 人が書くので。
71. **`株/` を一覧するとき `保有.md` を数に入れない。** 台帳は日ごとの値では
    ないのに、`株/*.md` を舐めると混ざる（日付の無い1枚が評価額 0 円の日に
    見える）。`YYYY-MM-DD.md` の形だけ拾う。

72. **足せるものだけ足した合計は、静かに小さくなる。** 株数の書けていない
    銘柄を 0 として足すと、「持っている額」が本当より小さく出て、しかも
    どこにも警告が出ない。**1つでも分からなければ合計を出さない**（`—` と
    出して、何が足りないかを名指しする）。個別の行は出してよい ——
    分かっているものは分かっている。
73. **門は「書く道」だけに置く。** 書きかけの台帳でも `--now` は見せてよい
    （拠点に書かないので、間違ったものが積まれない）。全部の入口で止めると、
    埋める前に何も確かめられなくなる。

### 読んだものを集める

76. **Chrome の `visit_time` は JS の安全な整数を超える。** 1601-01-01 からの
    マイクロ秒なので 1.3e16 になり、`Number.MAX_SAFE_INTEGER`（9.0e15）の外。
    `node:sqlite` は黙って丸めず `Value is too large to be represented as a
    JavaScript number` で投げる。**SQLite 側で ms に落としてから受け取る**
    （`v.visit_time/1000 - 11644473600000`）。
77. **履歴 DB は Chrome が起きているとロックされている。** `readOnly` で開いても
    `database is locked`。SQLite を読む側の作法どおり、一時ディレクトリへ写して
    から開いて、読み終えたら消す。
78. **Chrome の履歴は 90 日で消える。** 実測（2026-09-05）: Default に残って
    いたのは 2026-06-07 から。会話ログを `cleanupPeriodDays` で守ったのと同じ
    構図で、写しておかないと過去は取り返せない。そのぶん**「その日の履歴が
    0 件」は「その日は読まなかった」ではない** —— 0 件の日はファイルに触らない
    （落とし穴14 と同じ形。空を書くと消えた過去を「読まなかった日」で上書きする）。
79. **履歴をそのまま入れると、読み物はほとんど入らない。** 実測: 2 プロファイル
    29,645 訪問・ホスト 883 種のうち上位は、仕事の開発環境 3,965・localhost
    3,490・Google 検索 4,213・会社のポータル 800・漫画 459・不動産 290・
    証券口座 130。だから**拾う側を並べる**（allowlist）。除外を数え上げる形だと
    仕事のドメインが増えるたびに漏れ、漏れたものは `日記/` と `おすすめ/` の
    素材として LLM に渡ってしまう。台帳は手で書かない（原則4）——
    `投稿/` に出てくる外部リンクのホストが、そのまま allowlist になる。
80. **`github.com` は読み物ではなく作業。** allowlist に入る（投稿でシェア
    しているので）が、実測 2026-09-04 の 761 件はほとんどが仕事の private
    リポジトリで、**PR 題・issue 題・ファイルパスがそのまま拠点に入った**。
    リポジトリを見て回るのは `作業/` が既にコミットで持っている。技術記事のほうは
    `github.blog` で拾う。
81. **同じページが、URL 違いで何十行にもなる。** 実測: speakerdeck は
    `?slide=1..80` で1本のスライドが 80 行になり（804 件のうち大半）、
    検索欄に打った1文字ずつが `?draft=p` `?draft=po` … で 16 行になった。
    `/` と `/index.html` も別に数えられる。**畳む鍵は URL ではなくホストと題**
    にして、残すのはいちばん短い URL。
82. **`claude -p` に道具を持たせると、渡した素材の外を読みにいく。** 実測:
    許可が下りず「拠点のファイルを開こうとしたけど、読み取りの許可が
    下りてなかった」が**おすすめの1行目になった**（落とし穴47 と同じ形で、
    本文でないものが本文の頭に来る）。`--disallowed-tools` で封じて、
    「渡したものが全部」を機構のほうで保証する。**空の節も「無い」と書いて
    渡す** —— 黙って落とすと、渡していないのか読めなかったのかが向こうで
    区別できない。

### 複数の PC で使う

76. **Obsidian Sync はドットで始まるものを運ばない。** だから `.git`（19MB）も
    `.search.db`（148MB）も**機械ごとに別物**になる。索引は各機械で刻み直せば
    いいので好都合だが、git は2台で打つと同じ内容について2本の履歴ができる。
    **打つのは1台だけ**にする（`config.ts` の `commit`）。同じ理由で、
    素材置き場に**ドットを付けてはいけない** —— `.素材/` にすると同期されず、
    複数台にした意味が丸ごと消える。
77. **Obsidian Sync は `.json` を既定で運ばない。** 設定の「その他すべての形式」を
    on にしないと素材が届かず、しかも**黙って**片肺で畳むことになる
    （落とし穴14 と同じ形）。だから畳む道具は毎回「素材 N台（…）」と言う ——
    台数が1のままなら届いていない。
78. **日ごとに畳む部屋は、2台で走らせるとピンポンする。** `会話/` は jsonl と
    1:1（UUID 名）なので足し算になるが、`自分/` `アイボ/` `作業/` `読んだ/` は
    片方のログしか見ていない日を、もう片方が上書きする。jsonl を拠点に載せて
    解決はできない（実測 281MB。拠点 61MB の4倍を超える）。道具を2段に割って、
    運ぶのは素材（実測 11MB）だけにする。
79. **重複を落とすには鍵が要る。数だけの素材は、鍵が無いので2倍になる。**
    `アイボ/` の素材を「1日ぶんの集計」1件にしたら、同じセッションを2台が
    持っていたときに**数ごと2回足した**（実測 発言 3,554 → 7,112・道具
    9,694 → 19,395）。`deeds` と `told` は key で落ちていたのに、カウンタ
    だけ鍵を持っていなかった —— worktree を二重に数えた落とし穴17 と同じ形。
    素材を**セッションごと**（1本の jsonl につき1件）にして、`session` で
    落とす。鍵は絶対パスではなく**名前**にする（機械ごとに home が違う）。
    **確かめかたは「同じログを別名の2台目として置いて、1台のときと1バイトも
    違わないか」** —— これで見つけた。
80. **門で止まったのを「素材が無い」と言わない。** `outlook.ts` が、相手の
    機械が遅れているだけの夜に「株/ に値がまだ無い（先に stock.ts）」と
    嘘を言った。対象が 0 件になる理由が2つあるのに、文言が1つしか
    無かった（落とし穴14 と同じ形で、原因の違うものを1つの文言にしない）。

### hooks

36. **`.ts` は ESM なので `require` が無い。** Node が型注釈を剥がして直接
    実行するだけで、中身は ESM。`require("node:sqlite")` は `ReferenceError`。
    `hook.ts` は例外を全部飲むので、落ちても「なぜか鳴らない」としか見えない。
    静的 import に揃える。
37. **hook は全プロジェクトのセッションで鳴る。** 仕事のリポジトリでも鳴るので、
    配ってよいのは数と見出しまで。会話の原文は渡さない。原文が要るときは
    向こうが `search.ts` を叩いて取りに行く。
38. **鳴りすぎる hook は、鳴っていないのと同じ。** `前に＋動詞` を過去参照に
    入れると「この行の前に書いて」のような指示文で鳴る。語彙は
    `bin/hook.ts --check` で `自分/` に当てて決める（実測1.5%）。
39. **「何があっても 0 で終わる」は `main` の中だけでは守れない。** 読み込み
    時に落ちるもの（構文エラー・import の失敗）は try/catch の外で、
    終了コード 1 になる。`UserPromptSubmit` が非ゼロで終わると**そのプロンプト
    自体が止まる**ので、直したら登録する前に必ず1回叩く（実際に一度壊して、
    全部のセッションが止まる形になるのを見た）。

### 拠点そのもの

55. **部屋の名前を変えたら `.gitignore` も見る。** `.ruula.db` → `.search.db`
    の改名で除外が外れ、**144MB の索引が5世代ぶん履歴に入った**（`.git` が
    207MB）。定時便は `git add -A` なので、誰も気づかないまま毎晩積む。
    直したあとは名前ではなく **`*.db` で落とす** —— 次に名前を変えても効くように。
    掃除は `git filter-branch --index-filter` → `reflog expire` → `gc`
    （remote が無いので安全）。ただし `会話/` は再生成できない唯一の部屋なので、
    先にディレクトリごと `cp -a` して、あとで `diff -r` で突き合わせる。
56. **年の合計を「書けている月」から足し上げない。** `出来事/` は月ごとなので、
    まだ書いていない月があるとその年の合計が `プロフィール/年表/` と食い違う
    （実測: 2026 が 3,149 と 3,254 に割れた）。同じ年について違う数を言う部屋が
    できる —— 落とし穴21 と同じ形。年の合計は `プロフィール/年表/` から借りる。
57. **`posts.ts` は既にある記事を取りに行かない。** `updatedAt` が同じなら
    skip する（1,300本を毎晩取り直さないため）。だから部屋を `soto` →
    `投稿` に改名しても、**書いてある記事の frontmatter は `room: soto` のまま
    残る**（実測 1,307 本）。落とし穴22 と同じ形で、道具は消さないので改名した
    回は手で片付ける。いまのところ実害は無い —— 検索はディレクトリで部屋を
    決めるし、記事と SNS を分けるのは `source` のほう。

58. **`codex exec` は既定でセッションを残す。** `~/.codex/sessions` に rollout の
    jsonl が積まれるので、そのままだと**よその日記を書いたこと自体がその日の
    会話ログになり**、`sessions.ts` がそれを読んで `会話/` と `アイボ/` に混ざる（落とし穴46
    の Codex 版）。`--ephemeral` が要る。そして**返事を stdout から拾わない** ——
    起動情報も `hook: SessionStart` もトークン数も混ざる。`--output-last-message
    <file>` でファイルに取る。**読者を足すときは、その道具がログをどこに残すか
    先に見る。** 残さない口が無ければ足さない。

## 次にやること

### アイボ（相棒）—— 建て終わった（2026-09-04）

決めたこと:

- **相棒であってクローンではない。** polidog として喋るのではなく、
  横で見ていた側として喋る。「きのう、あれで3回つまずいてたよ」。
- **アイボは1匹。** Claude Code と Codex の記録を両方食べる。
  道具ごとに個体を分けない（記憶には `models:` のタグが付いている）。
- **「見た」と「読んだ」を分ける。** `会話/`（2026-08-28 〜）は一緒にいた
  ので断言してよい。`投稿/`（2004 〜）は後から読んだので伝聞として言う。
  アイボの誕生日は 2026-08-28。
- **`/aibo` は引数の有無で分ける。** 引数なしならその日の記録を読んで
  言いたいことを言う。引数があれば拠点を引いて答える。
  slash command は Markdown 1枚なので依存は増えない。
- **`/aibo` は拠点に書かない。** 仕事のリポジトリで呼ばれるので、書くと
  他所の話が混ざる（37）。書くのは夜の側。

建てたもの:

1. ~~**`アイボ/`（AI の行動記録）。**~~ **建てた**（2026-09-04）。
   8日ぶん・発言 3,116・道具 8,521。**発言の原文は写していない** ——
   890,227 文字あるが全部 `会話/` にあるので、写すと索引に同じ文が二重に
   載る。持たせたのは `会話/` にも `作業/` にも無いものだけ: 数（会話・
   発言・道具・モデル）、手下（サブエージェント）の働き、そして
   **道具の札**（実測 95% に付いている。`description` 84% ＋ パス 11%、
   平均 27 文字）。札は AI 自身が書いた要約なので短く、生のコマンドと違って
   鍵が写り込む心配もない。畳み方は `work.ts` から借りる（落とし穴15）。
2. ~~**「直された痕」を実測する。**~~ **実測して、文面判定は捨てた**
   （2026-09-04）。「違う」「やめて」「直して」で数えたら 645 発言中 51 件
   当たったが、中身を見ると本物は 1〜2 割だった（落とし穴44）。代わりに
   **機械的に確実な3つ**を `アイボ/` の `### 言われたこと` に入れた ——
   選ばせた 104・止められた 22・断られた 13（8日で 139 件 ≒ 17件/日）。
   別部屋にしなかったのは、これが「AI に対して起きたこと」で、その日の
   「やったこと」と並んでいたほうが文脈が繋がるから。
3. ~~**`日記/`（1日のまとめ）。**~~ **建てた**（2026-09-04）— `bin/diary.ts`。
   手元の `claude` を `-p` で呼ぶ最初の道具。書けたのは 7日ぶん
   （アイボの誕生日 2026-08-28 から、きのうまで）。1枚 400〜800字、
   実測 1枚 28 秒。ブラウザ・端末・検索・定時便に配線済み。
4. ~~**`/aibo`。**~~ **建てた**（2026-09-04）— `skills/aibo/SKILL.md`。
   引数なしならきょうの記録を読んで2〜4行言う。引数があれば `search.ts` で
   拠点を引いてから答える。**skill** として `~/.claude/skills/aibo` から
   symlink してあるので、どのリポジトリでも呼べる。外すときは symlink を
   消すだけ。
   **拠点には書かない**（読むだけ）。よそのリポジトリの原文は貼らず、
   名前と数までにとどめる（落とし穴37）。

### 残していること

- **推移の「まだ書いていないもの」をアイボへ移す。** あれは記録ではなく
  提案なので、本当はアイボの仕事。移し先ができるまで `プロフィール/推移/`
  に置いてある。
- **事典の人物・概念。** いまは `事典/プロジェクト/` だけ。分かち書きが
  要るので保留していたが、LLM に読ませるなら形態素解析は要らない
  （原則6の後半）。
- **週報と日記の配達。** いまは拠点に置くだけ。外に出す口は、原則4に並ぶ
  3例がどれもそこで止まっているので、読む習慣がついてから考える。
- **型検査を走らせていない。** `tsc` は npm 依存になるので入れていない。
  エディタの LSP 頼み。CI を建てるなら `npx -y typescript` を検討する。

### 定時便を触るとき

```bash
systemctl --user list-timers kyoten.timer   # 次にいつ起きるか
systemctl --user start kyoten.service       # いま1回流す
journalctl --user -u kyoten.service -o cat  # 何を言ったか
```

unit は `systemd/` にあり、`~/.config/systemd/user/` から symlink して
ある。直したら `systemctl --user daemon-reload`。

### 確認すること

書く道具は、2回流して `upd 0` になるか（＝冪等）:

```bash
bin/sessions.ts --quiet
bin/me.ts --quiet
bin/aibo.ts --quiet
bin/posts.ts --quiet
bin/work.ts --quiet
bin/reading.ts --quiet
bin/entities.ts --quiet
bin/profile.ts --quiet
bin/weekly.ts --quiet
bin/trend.ts --quiet
bin/search.ts --rebuild && bin/search.ts "検索語"
```

`/aibo` は拠点に書かないので、手順が通るかだけ見る:

```bash
cat skills/aibo/stance.md              # 立ち位置が読めるか
ls -l ~/.claude/skills/aibo            # symlink が生きているか
bin/diary.ts --try 2026-09-01          # 声を見る（拠点に書かない）
bin/search.ts "フォント" --room 日記 --limit 2   # 引けるか
```

日記と出来事は冪等ではないので、**2回流して `new 0 / ある N` になるか**を見る
（触っていなければ、それが「一度書いたら直さない」が効いている証拠）:

```bash
bin/diary.ts --dry-run    # 渡すものを見るだけ（API を叩かない）
bin/diary.ts --quiet
bin/diary.ts --quiet      # 2回目は new 0

bin/events.ts --dry-run --since 2026-08 --limit 1   # 渡す素材を見る
bin/events.ts --quiet
bin/events.ts --quiet     # 2回目は new 0

bin/news.ts --quiet
bin/news.ts --quiet       # 2回目は new 0（外の順位が動くので追記のみ）

bin/guest.ts --dry-run    # ニュースと記録を渡しているか（アイボの日記は渡さない）
bin/guest.ts --quiet
bin/guest.ts --quiet      # 2回目は new 0

bin/picks.ts --dry-run    # 話題と、根拠にするものを渡しているか
bin/picks.ts --quiet
bin/picks.ts --quiet      # 2回目は new 0
```

`手帳/` は道具が書かないので冪等の確認は要らない。見るのは**読めるか**と、
**アイボに背景として届いているか**:

```bash
bin/search.ts "語" --room 手帳            # 索引に載っているか
bin/web.ts --port 8841 --no-open &
curl -s "localhost:8841/api/tree?path=%E6%89%8B%E5%B8%B3"   # 歩けるか
fuser -k 8841/tcp
```

日記に届いているかは、`--dry-run` では見えない（あれは「これから書く日」しか
出さないので、全部書いてあると何も出ない）。**`import` してはいけない** ——
`diary.ts` は `import.meta.main` で守っていないので、引数なし本番実行になる
（落とし穴21）。写しを作って `buildPrompt` だけ叩く:

```bash
S=/tmp/kyoten-probe; mkdir -p "$S"; cp bin/*.ts config.ts "$S/"
sed 's/^process.exit(main());/export { buildPrompt };/' bin/diary.ts > "$S/probe.ts"
cd "$S" && node -e 'import("./probe.ts").then(async (m) => {
  const { readText } = await import("./util.ts");
  const p = m.buildPrompt("2026-09-04", readText(process.env.HOME +
    "/ghq/github.com/polidog/kyoten/skills/aibo/stance.md"));
  console.log(p.includes("### 手帳") ? "手帳 ✓" : "手帳 ✗");
})'
```

`読んだ/` は Chrome の履歴を見るので、**何が入るのかを先に目で見る**
（落とし穴79〜81。仕事のものが入っていたら台帳か `NEVER` を見直す）:

```bash
bin/reading.ts --hosts                       # allowlist を出すだけ
bin/reading.ts --dry-run                     # どのホストが何件入るか
bin/reading.ts --show 2026-09-04             # 1日ぶんの中身（拠点に書かない）
bin/reading.ts --show 2026-09-04 --source posts
```

おすすめの中身は**構えを割っていないか**で見る（落とし穴は数ではなく言いかた）:
読んでいない本文のことを言っていないか、「読め」と言っていないか、
**繋がらないものを無理に拠点へ結びつけていないか**（「これは繋がらないけど」
と言って出すのは正しい）。

X はアーカイブを展開してから、**まず書かずに**確かめる（落とし穴59〜62）:

```bash
ls ~/Documents/twitter-archive/data/tweets.js   # 展開できているか
bin/posts.ts --source x --dry-run               # 何日ぶんになるか
bin/posts.ts --source x
bin/posts.ts --source x --quiet                 # 2回目は upd 0（冪等）
```

落としていないうちは黙って飛ぶので、**`bin/posts.ts` の一覧に `x` が出ない**のが
正しい（`とどかず x` と出るなら名指ししている）。

株は、外に取りに行くので**取れているか**と、**2回流して upd 0 か**を見る:

```bash
bin/stock.ts --now          # いまの値（拠点に書かない）
bin/stock.ts --quiet
bin/stock.ts --quiet        # 2回目は new 0
```

冪等は「2回流す」だけでは足りない —— 書いたものに触らないだけかもしれない。
**その日を手で消してから書き直して、1バイトも違わないか**を見る
（落とし穴60 はこれで見つけた）:

```bash
F=~/Documents/Obsidian/kyoten/株/2026-09/2026-09-03.md
md5sum "$F"; rm "$F"; bin/stock.ts --quiet; md5sum "$F"   # 同じであること
```

見立ては日記と同じで冪等ではないので、**2回流して `new 0 / ある N`** を見る:

```bash
bin/outlook.ts --dry-run    # 渡すものを見るだけ（API を叩かない）
bin/outlook.ts --quiet
bin/outlook.ts --quiet      # 2回目は new 0
bin/browse.ts --plain 株    # 値の一覧に「見立てあり」が付くか
bin/browse.ts --plain 株 2026-09-03 | tail -12   # 値の下に見立てが続く
```

ブラウザは、**まとめ・一覧・1枚**の3か所を見る（帯の「株」から辿れる）。
持っていないときに壊れないかも見る —— `保有.md` を置く前が既定の状態なので:

```bash
bin/web.ts --port 8834 --no-open &
curl -s localhost:8834/api/summary | head -c 120   # stock は null でよい
curl -s "localhost:8834/api/list?room=stock"       # [] でよい（画面は始めかたを出す）
fuser -k 8834/tcp
```

見立ての中身は**構えを割っていないか**で見る（落とし穴は数ではなく言いかた）:
「買え」「売れ」が出ていないか、**上がった理由を作っていないか**、
「来週は戻る」と言い切っていないか。

よその日記を書かせたら、**codex のログに漏れていないか**も見る（落とし穴58）。
0 でなければ `--ephemeral` が効いていない:

```bash
python3 -c "
import json,glob
n=0
for p in glob.glob('$HOME/.codex/sessions/**/*.jsonl', recursive=True):
    if 'よその犬の立ち位置' in open(p, encoding='utf-8', errors='ignore').read(): n+=1
print('よその日記が漏れたセッション:', n)
"
```

口調を触ったら、**書いてあるところで試し書きして**声を見る（拠点には書かない）:

```bash
bin/diary.ts --try 2026-09-01
bin/events.ts --try 2026-08
bin/guest.ts --try 2026-09-02
```

よその犬の声は**アイボと混ざっていないか**で見る。アイボは常体、よそは ですます。
同じ画面に並ぶので、混ざると誰が喋っているか分からなくなる。そして
**こじつけを書いていないか** —— 繋がっていない日に繋げていたら、そこが崩れている:

```bash
bin/browse.ts --plain 日記 2026-09-02 | tail -20   # 日記の下によその日記が続く
bin/browse.ts --plain ニュース 2026-09-04 | head   # HN とはてなが両方あるか
```

年表は形が決まっているので、崩れていないかも見る（★ は1か月に1つまで）:

```bash
bin/browse.ts --plain 年表 | head -30
```

複数の PC で使うなら、**畳んだ結果が台数に依らないこと**を見る。同じログを
別名の2台目として置いて、1台のときと1バイトも違わなければ通っている
（落とし穴79 はこれで見つけた）:

```bash
K=~/Documents/Obsidian/kyoten
for t in me aibo work reading; do bin/$t.ts --fold-only --quiet; done
for r in 自分 アイボ 作業 読んだ; do cp -a "$K/$r" /tmp/$r.1台; done

# 同じログを laptop として集めて、2台ぶんを畳む（＝全部が重複）
for t in me aibo work reading; do KYOTEN_MACHINE=laptop bin/$t.ts --collect-only --quiet; done
for t in me aibo work reading; do bin/$t.ts --fold-only --quiet; done

for r in 自分 アイボ 作業 読んだ; do diff -rq /tmp/$r.1台 "$K/$r"; done   # 何も出ないこと
rm -rf "$K/素材/laptop"                                                  # 片付ける
```

追記のみの部屋の門も見る。遅れている機械を建てて、**止まって、理由を言う**か:

```bash
mkdir -p "$K/素材/laptop"
printf '{"機械":"laptop","集めた":"2026-08-30"}\n' > "$K/素材/laptop/見た.json"
bin/diary.ts --quiet    # 「素材が揃っているのは 2026-08-30 まで（laptop が遅れている）」
bin/me.ts --quiet       # 冪等な部屋は止まらない（素材 2台 と出る）
rm -rf "$K/素材/laptop"
```

機械ごとの振り分けは、偽の hostname で確かめる（`config.ts` に無い機械は
集めるだけになり、そう名乗る）:

```bash
KYOTEN_MACHINE=laptop bin/nightly.ts --dry-run --no-commit
bin/doctor.ts                                   # ✓ / ・ / ✗ が揃っているか
KYOTEN_MACHINE=poli-omarchy-intel bin/doctor.ts  # 向こうの機械から見た姿
```

`sessions.ts` と `aibo.ts` は `upd` が 0 にならない —— いま喋っている
セッション自身のログが伸び続けるため。**`reading.ts` も同じ**で、きょうの
ぶんはブラウザを使うたびに伸びる（過去の日は動かない）。それ以外が 0 なら
通っている。

まとめて流すなら `bin/nightly.ts`。順番に意味がある（2階は拠点に書かれた
ものを畳むので必ず後ろ。推移は週報の `scan` / `fold` を借りるので週報の次）。

読む道具は拠点に書かないので、冪等ではなく描けるかを見る:

```bash
bin/browse.ts --plain プロフィール    # 枠と数が揃っているか
bin/browse.ts --plain さがす 冪等      # 抜粋に検索語が残っているか
bin/browse.ts --plain 推移            # 累計が積み上がって見えるか
bin/browse.ts --plain 日記            # 一覧に書き出しが並ぶか
bin/browse.ts --plain 日記 2026-08-31 # 本文が折り返して読めるか
bin/web.ts --no-open &                # 立ち上げて
curl -s localhost:8823/api/summary | head -c 200
curl -s localhost:8823/api/summary | grep -o '"nightly":[^}]*}'  # つぎに日記が書かれる時刻
curl -s "localhost:8823/api/list?room=stock" | head -c 200      # 株の一覧
curl -s "localhost:8823/api/tree?path=" | head -c 200         # 部屋を歩く口
curl -s "localhost:8823/api/search?q=%E5%86%AA%E7%AD%89" | head -c 200
fuser -k 8823/tcp                     # 止めるとき（pkill は自分も殺す）
```

見た目を確かめたいときは headless の chromium で撮る（拡張は 127.0.0.1 を
開けなかった）:

```bash
chromium --headless=new --no-sandbox --hide-scrollbars \
  --window-size=1000,1400 --virtual-time-budget=5000 \
  --screenshot=/tmp/kyoten.png http://127.0.0.1:8823/
```

住所に日本語をそのまま入れると Node の HTTP パーサが 400 を返す。curl で
叩くときは percent-encode する（ブラウザ側は `encodeURIComponent` で組む）。

hook は、鳴るかと、黙って終わるかを見る:

```bash
echo '{"hook_event_name":"SessionStart","cwd":"'$PWD'"}' | bin/hook.ts
echo '{"hook_event_name":"UserPromptSubmit","prompt":"あの話どこだっけ"}' | bin/hook.ts
echo 'こわれた JSON' | bin/hook.ts; echo "終了コード $?"   # 0 であること
bin/hook.ts --check                                      # 鳴りすぎていないか
```

`自分/` を触ったら、混入が戻っていないかも見る:

```bash
grep -c '<command-name>\|<local-command\|<task-notification>' \
  ~/Documents/Obsidian/kyoten/自分/*/*.md   # すべて 0 であること
```

なお、**この環境の `grep` は日本語のパターンを取りこぼす**ことがある。
拠点の中身や日本語の語で探すときは `bin/search.ts` か Python を使う。

## 使い方

```bash
bin/sessions.ts             # 会話を写す
bin/sessions.ts --dry-run   # 書かずに結果だけ
bin/sessions.ts --since 2026-08-01
bin/sessions.ts --quiet     # 1行だけ（定時便用）
bin/sessions.ts --file <jsonl>   # 1本だけ（hook の SessionEnd 用）

bin/me.ts                   # 自分の発言を抜く（引数は sessions と同じ）
bin/aibo.ts                 # AI が何をしたか（引数は sessions と同じ）

# 複数の PC で使うとき、1階の4本は2段に割れている（`machine.ts`）
bin/me.ts --collect-only    # 手元のログ → 素材/<hostname>/ に置くだけ
bin/me.ts --fold-only       # 素材/*/ → 部屋 に畳むだけ
                            # 既定は両方。me / aibo / work / reading が同じ
                            # KYOTEN_MACHINE で機械の名前を差し替えられる
                            # （偽の2台目を建てて畳みを試すとき）
                            # KYOTEN_GHQ / KYOTEN_CHROME で置き場を変えられる

bin/posts.ts                # 投稿を集める（引数は sessions と同じ）
bin/posts.ts --source bluesky              # ソースを絞る（blog/bluesky/misskey/x）
bin/posts.ts --site http://127.0.0.1:8000  # 手元の polidog.jp を見る
bin/posts.ts --source x                    # X は手元のアーカイブから読む
                            # 展開先は KYOTEN_X_ARCHIVE（既定
                            # ~/Documents/twitter-archive）。落としていなければ
                            # 黙って飛ばす（名指ししたときだけ無いと言う）

bin/work.ts                 # 作業（引数は sessions と同じ）
bin/entities.ts             # 事典（--dry-run / --quiet）
bin/profile.ts              # プロフィール・スキル・年表（同上）
bin/weekly.ts               # 週報（同上）
bin/trend.ts                # 推移（同上）
bin/diary.ts                # 日記。アイボが書く（--dry-run / --since / --quiet）
bin/diary.ts --try 2026-09-02    # 拠点に書かずに1枚書かせる（声を見るとき）
                            # KYOTEN_MODEL で書き手を変えられる（既定 claude-opus-5）
                            # 書き直したいときは、その日のファイルを手で消す

bin/events.ts               # 年表。アイボが月ごとに書く（--dry-run / --quiet）
bin/events.ts --since 2026-01
bin/events.ts --limit 12    # 何か月ぶんで止めるか（236か月あるので）
bin/events.ts --try 2026-08      # 拠点に書かずに1枚書かせる（声を見るとき）
                            # 書くのは先月まで。今月はまだ終わっていない

bin/news.ts                 # ニュース。その日の IT の話題（--dry-run / --since / --quiet）
bin/news.ts --show 2026-09-02    # 拠点に書かずに1日ぶん出す（中身を見るとき）

bin/reading.ts              # 読んだもの。履歴と投稿から（--dry-run / --since / --quiet）
bin/reading.ts --source chrome   # ソースを絞る（chrome / posts）
bin/reading.ts --hosts      # allowlist を出すだけ（拠点に書かない）
bin/reading.ts --show 2026-09-04 # 1日ぶんを拠点に書かずに出す
                            # 見る先は KYOTEN_CHROME（既定 ~/.config/google-chrome。
                            # Default と Profile N を全部見る）。0 件の日は書かない

bin/picks.ts                # おすすめ。ニュースからアイボが選ぶ（--dry-run / --quiet）
bin/picks.ts --try          # 拠点に書かずに書かせる（声を見るとき）
bin/picks.ts --try 2026-09-04
                            # 選ぶのはいちばん新しい `ニュース/` の日だけ。
                            # 書き直したいときは、そのファイルを手で消す
                            # HN は過去も引ける。はてなは直近2日ぶんだけ

bin/guest.ts                # よその日記。よその犬が外から書く（--dry-run / --since / --quiet）
bin/guest.ts --try 2026-09-02    # 拠点に書かずに書かせる（声を見るとき）
                            # 書き手は codex（KYOTEN_CODEX で差し替えられる）
                            # 書き直したいときは、そのファイルを手で消す

bin/stock.ts                # 株。確定した終値と評価額（--dry-run / --quiet）
bin/stock.ts --now          # いまの値を出すだけ（場中。拠点には書かない）
bin/stock.ts --init         # 保有.md の雛形を出す
                            # 書くのは拠点にある最後の日より先だけ（過去は埋め戻さない）

bin/outlook.ts              # 見立て。アイボが株を見て言う（--dry-run / --since / --quiet）
bin/outlook.ts --try 2026-09-03  # 拠点に書かずに書かせる（声を見るとき）
                            # 書き直したいときは、そのファイルを手で消す

bin/nightly.ts              # 定時便（全部流して git commit まで）
bin/nightly.ts --dry-run
bin/nightly.ts --no-commit

bin/archive.ts              # reading-notes をアーカイブへ写す（元は消さない）
bin/archive.ts --dry-run

bin/search.ts "検索語"       # 素材が新しければ勝手に刻み直す
bin/search.ts "検索語" --room 自分 --project polidog/kyoten --since 2026-09-01
bin/search.ts --rebuild     # 刻み直すだけ
bin/search.ts --stats       # 索引の中身を数える
bin/search.ts --rebuild --quiet  # 刻み直して1行だけ（定時便用）

bin/web.ts                  # ブラウザで拠点を読む（127.0.0.1 固定）
bin/web.ts --port 9999 --no-open
                            # まとめ・一覧・原文・検索。`/` で検索欄へ

bin/browse.ts               # 端末で拠点を歩く
bin/browse.ts スキル php     # 部屋と行き先を指して始める
bin/browse.ts --plain 週報   # 対話せず1回だけ描く
bin/browse.ts --plain さがす 冪等

bin/doctor.ts               # この機械の具合を見る（読むだけ）
bin/doctor.ts --quiet       # 足りないものだけ
                            # ✓ できている / ・ まだ / ✗ 食い違い
                            # 直す口は skill の `/kyoten`

bin/hook.ts                 # 呼び方だけ出す（hooks から stdin で呼ばれる）
bin/hook.ts --check         # `自分/` に当てて、何割で鳴るか数える
```

`/aibo` は **skill**（`skills/aibo/SKILL.md`）。`~/.claude/skills/aibo` から
symlink してある —— ほかの21個と同じ置きかた（`<repo>/skills/<name>/SKILL.md`
を `~/.claude/skills/<name>` へ）。立ち位置と喋りかたは `skills/aibo/stance.md`
の1枚で、`diary.ts` と共有している。
よその犬の立ち位置は `stance/guest.md` の1枚で、`guest.ts` が読む。

`/kyoten` も **skill**（`skills/kyoten/SKILL.md`）で、同じ置きかた。こちらは
アイボではなく**この機械の面倒を見る口** —— 判定は `bin/doctor.ts` が全部やり、
skill は直しかたと、直してよいもの／いけないものだけを持つ。立ち位置の紙は
無い（喋る口ではないので）。

`/kabu` も **skill**（`skills/kabu/SKILL.md`）で、同じ置きかた。株のときの
構えは `skills/kabu/stance.md` の1枚で、`outlook.ts` と `/kabu` と
`kabu` subagent が共有する —— **声は足さない**。一人称も口調も
`skills/aibo/stance.md` のまま（株の話で別人が出てきたら相棒が2人に割れる）。

`kabu` **subagent**（`agents/kabu.md` → `~/.claude/agents/kabu.md`）は掘る側。
1銘柄の値動きを全期間から並べる、過去の見立てが当たったかを数える、決算を
外から確かめる、といった長い調べもの。**subagent だけが Web を見てよい** ——
そのぶん「見た（拠点の株）／読んだ（拠点のほか）／**調べた**（Web）」を
3つに分けて、調べたものには URL を添える。`outlook.ts` は拠点だけを見る
（夜に無人で走るので、外の当たり外れを持ち込まない）。

`--room` に渡せるのは
`会話 / 自分 / アイボ / 日記 / よその日記 / 出来事 / ニュース / おすすめ /
読んだ / 投稿 / 作業 / 株 / 見立て / 事典 / プロフィール / 週報 / 手帳 /
アーカイブ`。

`KYOTEN` 環境変数で拠点の場所を変えられる（既定 `~/Documents/Obsidian/kyoten`）。
X のアーカイブの場所は `KYOTEN_X_ARCHIVE`（既定 `~/Documents/twitter-archive`。
展開済みのディレクトリを指す —— zip のままでは読めない）。
reading-notes の場所は `KYOTEN_READING`。`archive.ts` はそこから
`アーカイブ/reading-notes/` へ追記的に写し、元から消えたファイルも勝手には消さない。

## 応答

日本語で。技術用語とコード識別子は原文のまま。
